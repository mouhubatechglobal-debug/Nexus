# services/agents — Service d'agents (à venir)

Microservice d'**orchestration d'agents** autonomes de NEXUS
(planification, outils, boucles d'exécution).

> Placeholder — sera implémenté dans un prompt ultérieur.
> Communiquera avec `services/ai` et `services/sandbox` via les contrats
> partagés et les files BullMQ définies dans `services/workers`.
