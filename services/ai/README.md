# services/ai — Service IA (à venir)

Microservice dédié aux capacités d'**intelligence artificielle** de NEXUS
(génération, embedding, orchestration de modèles).

> Placeholder — sera implémenté dans un prompt ultérieur.
> Conventions prévues : TypeScript, Fastify ou worker BullMQ, contrats
> partagés via `@nexus/contracts`, config via `@nexus/config`,
> logs via `@nexus/observability`.
