/**
 * Schéma Drizzle — point unique de définition des tables PostgreSQL.
 *
 * Volontairement vide à cette étape (Prompt 01 : structure de base).
 * Les tables métier seront ajoutées ici, puis les migrations générées :
 *   npm run db:generate
 *   npm run db:push
 */
export const TABLES = {} as const;
